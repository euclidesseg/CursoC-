using ClasesMetodos;

namespace Main
{
    public class MainRetorno
    {
     /*   public static void Main(string[] args)
        {
            int numero1 = 7, numero2 = 5;

            int suma;
            int resta;
            int multiplicacion;
            int division;

            RetornoValores retorno1 = new RetornoValores();
            suma = retorno1.sumar(numero1, numero2);
            resta = retorno1.restar(numero1, numero2);
            multiplicacion = retorno1.multiplicar(numero1, numero2);
            division = retorno1.dividir(numero1, numero2);

            retorno1.mostrar(suma, resta, multiplicacion, division);
        }*/
    }
}
